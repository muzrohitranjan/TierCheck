# TierCheck JOBS → Company Roles Fix
## Group jobs by company for role breakdown

### Plan:
1. [ ] Create this TODO-JOBS.md
2. [ ] Update companies.js 
   - `loadJobRoles()` → fetchJobs → groupBy company.name
   - `renderCompanies()` uses `jobRoles[c.name]`
3. [ ] Update companies.html DOMContentLoaded
4. [ ] Test: companies.html → roles from jobs table

**Jobs table → company roles mapping ready!**
